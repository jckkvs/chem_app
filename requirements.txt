# Core Framework
Django>=4.2
django-ninja>=1.0
gunicorn>=21.0

# Data Science
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
lightgbm>=4.0

# Visualization
matplotlib>=3.7
seaborn>=0.12
shap>=0.44

# ML Tracking
mlflow>=2.10

# Chemistry
rdkit>=2023.09
umap-learn>=0.5

# Frontend
streamlit>=1.30
requests>=2.31

# Task Queue
huey>=2.4

# Testing
pytest>=7.4
pytest-django>=4.5
pytest-cov>=4.1

# Optional: XTB (install via conda)
# xtb-python

# Optional: TARTE (Transformer tabular features)
# pip install tarte-ai

# Optional: GPT4All (Lightweight LLM Assistant)
# pip install gpt4all

# ========================================
# Optional: Advanced ML Libraries (25+ libraries)
# ========================================

# === Interpretable ML ===
# imodels>=1.3  # RuleFit, GreedyRuleList, BayesianRuleList, FIGS, HSTree
# wittgenstein>=0.3  # RIPPER/IREP rule-based algorithms
# skope-rules>=1.0  # Rule extraction
# corels  # Certifiably Optimal RulE ListS (requires compilation)
# anchor-exp>=0.0.2  # Anchor explanations
# alibi>=0.9  # Algorithm explanations

# === Tree Models (Specialized) ===
# rgf-python>=3.12  # Regularized Greedy Forest
# linear-tree>=0.3  # Linear Tree Models (hybrid)
# node-network  # Neural Oblivious Decision Ensembles
# pytorch-tabnet>=4.0  # TabNet deep learning

# === GAM (Generalized Additive Models) ===
# pygam>=0.8  # Generalized Additive Models
# interpret>=0.5  # Microsoft InterpretML (EBM - Explainable Boosting Machine)

# === Gradient Boosting Extensions ===
# catboost>=1.2  # CatBoost (categorical features optimized)
# ngboost>=0.4  # Probabilistic Gradient Boosting

# === Causal Inference ===
# econml>=0.14  # Microsoft EconML (causal ML)
# causalml>=0.14  # Uber CausalML
# dowhy>=0.9  # Causal graph inference

# === Deep Learning (Tabular Data) ===
# pytorch-tabular>=1.0  # PyTorch for tabular data
# rtdl>=0.0.13  # Research Tabular Deep Learning

# === Feature Engineering & Selection ===
# formulaic>=0.6  # R-style model formulas
# feature-engine>=1.6  # Feature engineering専門
# boruta>=0.3  # Boruta feature selection

# === Hyperparameter Optimization ===
optuna>=3.5  # Optuna HPO framework
# optuna-integration  # Additional integrations

# === Machine Learning Extensions ===
mlxtend>=0.22  # ML extensions (Stacking/Voting/Sequential FS/Plotting)

# ========================================
# Installation Commands
# ========================================

# Install Core Advanced ML (recommended):
# pip install imodels rgf-python linear-tree catboost ngboost optuna

# Install Interpretable ML Suite:
# pip install imodels wittgenstein skope-rules interpret pygam

# Install Causal Inference Suite:
# pip install econml causalml dowhy

# Install Deep Learning for Tabular:
# pip install pytorch-tabnet pytorch-tabular

# Install ALL optional libraries (may take time):
# pip install imodels wittgenstein skope-rules interpret pygam \
#             rgf-python linear-tree catboost ngboost \
#             econml causalml dowhy \
#             pytorch-tabnet pytorch-tabular rtdl \
#             formulaic feature-engine boruta optuna

# Note: Some libraries (corels, node-network) may require additional setup
