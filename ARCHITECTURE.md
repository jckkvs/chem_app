# アーキテクチャドキュメント

Chemical ML Platformの設計思想、モジュール構成、拡張ポイントを説明します。

## 🏗️ システム全体像

```
┌─────────────────────────────────────────────────────────┐
│                    Frontend Layer                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │   Django     │  │  Streamlit   │  │     PWA      │  │
│  │  Templates   │  │     App      │  │   (Mobile)   │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
┌─────────────────────────────────────────────────────────┐
│                     API Layer                            │
│  ┌──────────────────────────────────────────────────┐   │
│  │         Django Ninja REST API (16 endpoints)     │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                           │
┌─────────────────────────────────────────────────────────┐
│                   Service Layer                          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │
│  │  Features   │  │     ML      │  │     Vis     │     │
│  │ (26 modules)│  │ (32 modules)│  │ (10 modules)│     │
│  └─────────────┘  └─────────────┘  └─────────────┘     │
└─────────────────────────────────────────────────────────┘
                           │
┌─────────────────────────────────────────────────────────┐
│                    Data Layer                            │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │ SQLite   │  │  MLflow  │  │   Huey   │              │
│  │   DB     │  │ Tracking │  │  Queue   │              │
│  └──────────┘  └──────────┘  └──────────┘              │
└─────────────────────────────────────────────────────────┘
```

---

## 📐 設計原則

### 1. **Strategy Pattern（戦略パターン）**
異なる特徴量抽出器・MLモデルを統一インターフェースで扱う

```python
# 統一されたインターフェース
class BaseFeatureExtractor(ABC):
    def fit(self, smiles_list, y=None) -> Self
    def transform(self, smiles_list) -> pd.DataFrame
    def fit_transform(self, smiles_list, y=None) -> pd.DataFrame
```

### 2. **Dependency Injection（依存性注入）**
コンポーネント間の結合度を低減

```python
# Pipeline が具体的な実装に依存しない
pipeline = MLPipeline(
    feature_extractor=RDKitFeatureExtractor(),  # 差し替え可能
    model=XGBoostRegressor(),                   # 差し替え可能
)
```

### 3. **Plugin Architecture（プラグインアーキテクチャ）**
新機能を外部から追加可能

```python
# プラグインとして機能を追加
plugin_manager.register(my_custom_plugin)
```

---

## 📦 モジュール構成

### `core/services/features/` - 特徴量抽出（26モジュール）

| モジュール | 説明 | 依存関係 |
|----------|------|---------|
| `base.py` | 抽象基底クラス | なし |
| `rdkit_eng.py` | RDKit分子記述子 | RDKit |
| `xtb_eng.py` | 量子化学記述子 | XTB (optional) |
| `uma_eng.py` | UMAP埋め込み | UMAP |
| `tarte_eng.py` | Transformer埋め込み | tarte-ai (optional) |
| `smart_feature_engine.py` | 物性別最適特徴量 | 複数 |

**拡張ポイント**: `BaseFeatureExtractor`を継承して新しい抽出器を作成

### `core/services/ml/` - 機械学習（32モジュール）

| モジュール | 説明 | 主要クラス |
|----------|------|-----------|
| `pipeline.py` | MLパイプライン | `MLPipeline` |
| `automl.py` | Optunaハイパーパラメータ最適化 | `AutoMLOptimizer` |
| `uncertainty.py` | 不確実性定量化 | `UncertaintyQuantifier` |
| `active_learning.py` | 能動学習 | `ActiveLearner` |
| `batch_predict.py` | バッチ予測 | `BatchPredictor` |

**拡張ポイント**: 新しいモデルやアルゴリズムを追加

### `core/services/vis/` - 可視化（10モジュール）

| モジュール | 説明 |
|----------|------|
| `shap_eng.py` | SHAP説明 |
| `pdp_eng.py` | Partial Dependence Plot |
| `mol_viewer.py` | 分子構造ビューア |
| `chem_space.py` | 化学空間マップ |

**拡張ポイント**: 新しい可視化手法を追加

---

## 🔌 主要な拡張ポイント

### 1. 特徴量抽出器の追加

**目的**: 新しい分子記述子・埋め込み手法を追加

**手順**:
1. `core/services/features/my_extractor.py` を作成
2. `BaseFeatureExtractor` を継承
3. `transform()` メソッドを実装
4. `__init__.py` でエクスポート

**テンプレート**: `templates/feature_extractor_template.py`

### 2. MLモデルの追加

**目的**: 新しいアルゴリズムや事前学習モデルを追加

**手順**:
1. `core/services/ml/my_model.py` を作成
2. `BaseMLModel` を継承（作成予定）
3. `fit()`, `predict()` を実装

### 3. プラグインの追加

**目的**: 外部から機能を追加

**手順**:
1. `plugins/my_plugin/` ディレクトリ作成
2. `plugin.py` でプラグイン定義
3. `create_plugin()` 関数を実装

**ガイド**: [docs/PLUGIN_DEVELOPMENT.md](docs/PLUGIN_DEVELOPMENT.md)

### 4. APIエンドポイントの追加

**目的**: 新しいREST APIを追加

**手順**:
1. `core/api.py` に `@api.post()` デコレータで関数追加
2. リクエスト・レスポンススキーマを定義
3. `core/tests/test_api.py` にテスト追加

---

## 🔄 データフロー

### 予測リクエストの流れ

```
User Request (Frontend)
    ↓
Django View / API Endpoint
    ↓
Service Layer (MLPipeline)
    ↓
Feature Extraction (RDKitFeatureExtractor)
    ↓
Model Prediction (XGBoost)
    ↓
Uncertainty Quantification
    ↓
Response (JSON)
    ↓
Frontend Display
```

### 学習フローの流れ

```
User Upload CSV
    ↓
Data Validation
    ↓
Feature Engineering (SmartFeatureEngine)
    ↓
AutoML (Optuna)
    ↓
Model Training & Evaluation
    ↓
MLflow Logging
    ↓
Model Persistence
    ↓
Experiment Result Display
```

---

## 🗄️ データベーススキーマ

### 主要モデル

```python
# core/models.py

class Dataset(models.Model):
    name = models.CharField(max_length=200)
    file = models.FileField(upload_to='datasets/')
    uploaded_at = models.DateTimeField(auto_now_add=True)
    row_count = models.IntegerField()

class Experiment(models.Model):
    dataset = models.ForeignKey(Dataset)
    name = models.CharField(max_length=200)
    target_column = models.CharField(max_length=100)
    feature_type = models.CharField(max_length=50)
    model_type = models.CharField(max_length=50)
    created_at = models.DateTimeField(auto_now_add=True)
    status = models.CharField(max_length=20)
    
class ExperimentResult(models.Model):
    experiment = models.OneToOneField(Experiment)
    metrics = models.JSONField()
    model_path = models.CharField(max_length=500)
```

詳細: [docs/DATA_SCHEMA.md](docs/DATA_SCHEMA.md)

---

## 🧩 依存関係管理

### コア依存関係（必須）
- Django, django-ninja
- pandas, numpy, scikit-learn
- RDKit, UMAP
- XGBoost, LightGBM
- MLflow, Huey

### オプショナル依存関係
- XTB (量子化学計算)
- tarte-ai (Transformer特徴量)
- Uni-Mol (3D分子埋め込み)
- ChemBERTa (SMILES Transformer)

**設計**: オプショナル依存がなくてもコアは動作

```python
# 例: tarte_eng.py
try:
    import tarte
    TARTE_AVAILABLE = True
except ImportError:
    TARTE_AVAILABLE = False

def transform(self, smiles_list):
    if not TARTE_AVAILABLE:
        logger.warning("TARTE not installed, returning NaN")
        return pd.DataFrame(...)  # フォールバック
```

---

## 🚀 パフォーマンス最適化

### 1. **キャッシング**
- RDKit分子オブジェクトのキャッシュ（`core/services/cache.py`）
- 計算済み特徴量のキャッシュ

### 2. **非同期処理**
- Hueyタスクキューで重い計算をバックグラウンド実行
- `core/tasks.py` でタスク定義

### 3. **バッチ処理**
- 大量分子の一括特徴量抽出
- `core/services/utils/batch_processing.py`

---

## 🔒 セキュリティ

- **入力検証**: SMILES/CSVファイルのバリデーション
- **サンドボックス**: ユーザー提供コードの実行制限
- **レート制限**: API呼び出し頻度制限（実装予定）

---

## 📊 監視とロギング

- **MLflow**: 実験トラッキング、メトリクス記録
- **Django Logging**: アプリケーションログ
- **エラー追跡**: スタックトレース保存

---

## 🔮 将来の拡張計画

1. **モデルレジストリ**: 複数バージョンのモデル管理
2. **A/Bテスト**: モデル性能比較機能
3. **説明可能AI統合**: SHAP/LIME統一インターフェース
4. **GraphQL API**: より柔軟なクエリ対応
5. **マルチテナント**: 組織単位のデータ分離

---

## 📚 関連ドキュメント

- [CONTRIBUTING.md](CONTRIBUTING.md) - 貢献ガイド
- [docs/PLUGIN_DEVELOPMENT.md](docs/PLUGIN_DEVELOPMENT.md) - プラグイン開発
- [docs/API_GUIDELINES.md](docs/API_GUIDELINES.md) - API設計
- [README.md](README.md) - プロジェクト概要
